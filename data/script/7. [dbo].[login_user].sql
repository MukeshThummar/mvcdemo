

IF OBJECT_ID('[dbo].[user]') IS NULL
BEGIN
	SET ANSI_NULLS ON

	SET QUOTED_IDENTIFIER ON

	SET ANSI_PADDING ON

	CREATE TABLE [dbo].[login_user](
		[ID] [int] IDENTITY(1000,1) NOT NULL,
		[login_user_name]	VARCHAR(64) NOT NULL,
		[login_password] VARCHAR(128) NOT NULL
	PRIMARY KEY CLUSTERED 
	(
		[ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
	) ON [PRIMARY]

	SET ANSI_NULLS OFF

	SET QUOTED_IDENTIFIER OFF

	SET ANSI_PADDING OFF

	INSERT INTO dbo.login_user(login_user_name, login_password) VALUES ('admin','admin')
END


